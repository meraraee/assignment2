#include <iostream>
using namespace std;

int main08(){
    
    
    // in order for z to cout as a double we just need to initializ one of the given values, x or y to a double
    double x = 10;
    int y = 3;
    
    double z = x/y;
    
    cout << z << endl;
    
    
    return 0;
}
